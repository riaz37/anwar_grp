// Stub for the "server-only" package under Vitest (plain Node, not the
// Next.js webpack bundler that provides the real alias). Mirrors what
// Next's compiled shim does at build time: a no-op import guard.
export {};
